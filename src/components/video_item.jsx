import React, { memo } from "react";

const VideoSnippet = memo((props) => {
  // const [title] = props.;
  console.log(props);

  return (
    <>
      <p>wow</p>
    </>
  );
});

export default VideoSnippet;
